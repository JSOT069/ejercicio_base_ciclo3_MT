@page
@model ProyectoCiclo3.App.Frontend.Pages.FormRutaModel
@{
}
 
<div class="text-center">
<form method="POST">
  @*<label for="id" class="form-label">id:</label>
  <input type="number" class="form-control" id="id" name="id" placeholder="id">*@
  <label for="Origen" class="form-label">Origen:</label>
  <select asp-for="Ruta.origen" class="form-control" required>
        <option value="">Selecciona el origen</option>
        @foreach (var estacion in Model.Estaciones)
    {
        <option value="@estacion.id">@estacion.nombre</option>
    }
    </select>
  <label for="Destino" class="form-label">Destino:</label>
  <select asp-for="Ruta.destino" class="form-control" required>
        <option value="">Selecciona el destino</option>
        @foreach (var estacion in Model.Estaciones)
    {
        <option value="@estacion.id">@estacion.nombre</option>
    }
    </select>
  <label for="Tiempo estimado" class="form-label">Tiempo estimado:</label>
  <input type="text" class="form-control" id="tiempo_estimado" asp-for='Ruta.tiempo_estimado' placeholder="Tiempo estimado">
  <hr>
  <button type="submit" class="btn btn-success">Enviar</button>
</form>
</div>